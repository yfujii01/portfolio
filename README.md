# README

## 起動方法

$ docker-compose up -d

## ログの確認

$ docker-compose logs -f

## 停止

$ docker-compose stop

## 再開

$ docker-compose start

## ユーザー登録について

/app/models/user.rb

:registerableををコメントアウトしているため、新規追加はできなくしている
